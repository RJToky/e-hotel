<?php
    // include("./fonction.php");
    // $con = getConnection();

    echo $_POST['types'];

    // $idType = getIdType($con, $_POST['type']);
    // $nbChambre = $_POST['nbChambre'];
    // $loyer = $_POST['loyer'];
    // $quartier = $_POST['quartier'];
    // $desc = $_POST['desc'];
    // $photo = uploadImage($_FILES['photo']);

    // addHabitation($con, $idType, $nbChambre, $loyer, $quartier, $desc, $photo);

?>